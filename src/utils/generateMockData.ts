/**
 * Mock Data Generator Utility
 * Generates comprehensive, interconnected mock data for all PRD models
 * Ensures all attributes are covered with realistic, randomized data
 */

// This file will be used to generate comprehensive mock data
// For now, we'll ensure existing mock files are comprehensive and create missing ones

export const MOCK_DATA_CONFIG = {
  // Ensure numeric IDs throughout
  NUMERIC_IDS: true,
  // Use nested relationships (not joins)
  NESTED_RELATIONSHIPS: true,
  // Randomization seed for consistency
  RANDOM_SEED: 12345,
};


